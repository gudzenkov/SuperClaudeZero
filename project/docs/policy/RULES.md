# Project Rules

Project-specific behavioral rules that supplement global rules in `~/.claude/policy/RULES.md`.

---

*Add project-specific rules here. Examples:*
- *"All database migrations must be reversible"*
- *"No direct DOM manipulation outside React components"*
- *"API responses must follow the ErrorResponse schema for errors"*
- *"Feature flags required for all new user-facing features"*
